forrange [
    0
    { 1 + swap dup 2 dupgrab swap () swap } 2 grab *
    () drop drop
] :=

\n "
" :=

dropall {
    size { drop } * ()
} :=

rev { size 1 - 0 $ { 1 + dup 1 + grab swap } * () drop } :=
