[
    0
    { 1 + swap dup 2 dupgrab swap () swap } 2 grab *
    () drop drop
] forrange :=

"
" \n :=

{
    size { drop } * ()
} dropall :=

{ size 1 - 0 $ { 1 + dup 1 + grab swap } * () drop } rev :=
{ len 1 - { + } * swap () size 1 - grab () } sumBlock :=
